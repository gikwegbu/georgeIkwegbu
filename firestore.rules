rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read access for portfolio data
    // Write access restricted to authenticated user
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
